spec:
  name: michiganflyers-election
  services:
  - name: web
    git:
      repo_clone_url: https://github.com/michiganflyers/elections.git
      branch: do-database
    dockerfile_path: Containerfile
    envs:
    - key: ELECTIONDB_URL
      scope: RUN_TIME
      value: ${election-data.DATABASE_URL}
  databases:
  - name: election-data
